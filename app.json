{
  "name": "whatsapp-media-bot",
  "description": "Bot de WhatsApp para IA y medios.",
  "repository": "https://github.com/guianpierrcastillolazo-rgb/whatsapp-media-bot",
  "keywords": [
    "whatsapp",
    "baileys",
    "ai",
    "media",
    "bot"
  ],
  "stack": "heroku-24",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "env": {
    "AI_PROVIDER": {
      "description": "AI provider: local, ollama, llama_cpp, localai, gemini, groq, mistral or openrouter.",
      "value": "groq",
      "required": false
    },
    "AI_MODEL": {
      "description": "Model name for the selected AI provider.",
      "value": "openai/gpt-oss-20b",
      "required": false
    },
    "GROQ_API_KEY": {
      "description": "Optional Groq API key for !ai.",
      "required": false,
      "value": ""
    },
    "GEMINI_API_KEY": {
      "description": "Optional Gemini API key for !ai.",
      "required": false,
      "value": ""
    },
    "MISTRAL_API_KEY": {
      "description": "Optional Mistral API key for !ai.",
      "required": false,
      "value": ""
    },
    "OPENROUTER_API_KEY": {
      "description": "Optional OpenRouter API key for !ai.",
      "required": false,
      "value": ""
    },
    "GROUPS_ENABLED": {
      "description": "Allow commands in WhatsApp groups.",
      "value": "true",
      "required": false
    },
    "REPLY_IN_PRIVATE": {
      "description": "Allow commands in private chats.",
      "value": "true",
      "required": false
    },
    "PAIRING_CODE": {
      "description": "WHATSAPP_NUMBER has priority and uses pairing code; leave WHATSAPP_NUMBER empty to use QR.",
      "value": "false",
      "required": false
    },
    "WHATSAPP_NUMBER": {
      "description": "WhatsApp number with country code, digits only, for pairing code.",
      "required": false,
      "value": ""
    },
    "NSFW_ENABLED": {
      "description": "Set true only to enable adult-image commands in explicitly allowed groups.",
      "value": "false",
      "required": false
    },
    "NSFW_ALLOWED_GROUPS": {
      "description": "Optional comma-separated numeric IDs of groups allowed to request adult images; empty allows all groups.",
      "value": "",
      "required": false
    },
    "NSFW_ALLOW_EXTERNAL_URLS": {
      "description": "Allow image URLs from HTTP/HTTPS hosts outside the API domain.",
      "value": "false",
      "required": false
    },
    "NSFW_ALLOW_PRIVATE_CHATS": {
      "description": "Allow adult-image commands in private chats.",
      "value": "false",
      "required": false
    },
    "CONTROL_API_TOKEN": {
      "description": "Leave empty to disable the local control API on Heroku.",
      "required": false,
      "value": ""
    },
    "AUTH_DIR": {
      "description": "Directory for WhatsApp credentials. Heroku storage is ephemeral.",
      "value": "auth_info",
      "required": false
    }
  }
}
